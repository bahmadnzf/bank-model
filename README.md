# bank-model
Bank Loan — Classification Notebook  This notebook performs a complete classification pipeline to predict whether a customer **gets a loan** or **not** using the provided `bankloan.csv` dataset. It includes EDA, preprocessing (encoding, feature engineering, scaling), model training for several models, evaluation, and a final comparison table.  
